import { ListOfUsersImplementation } from './list-of-users-implementation';

describe('ListOfUsersImplementation', () => {
  it('should create an instance', () => {
    expect(new ListOfUsersImplementation()).toBeTruthy();
  });
});
